{
  "name": "simple-ledger-app",
  "version": "1.0.0",
  "description": "A lightweight, in-memory transactional ledger API for managing accounts and fund transfers with strict atomicity guarantees. Built with Java 11, Spring Boot, and Maven.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
